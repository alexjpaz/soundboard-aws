const Promise = require('bluebird');

function WebDriver() {
  this.execute = function(config) {
    :w
  };

  this.playAudio = function(config) {

  };

  this.playVoice = function(config) {
  };
}

module.exports = WebDriver;
